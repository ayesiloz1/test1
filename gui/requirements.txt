"""
PyQt5 GUI requirements - Pinned versions for security
"""

# GUI Framework
PyQt5==5.15.10

# Deep Learning
torch==2.1.2
torchvision==0.16.2

# Core Dependencies
numpy==1.26.3
opencv-python==4.9.0.80
Pillow==10.2.0

# LLM Integration with Azure OpenAI
openai==1.10.0
python-dotenv==1.0.1

# Additional Security Note:
# For production deployment with hash verification, use:
# pip install --require-hashes -r requirements.txt
# Then generate hashes with: pip-compile --generate-hashes requirements.in
